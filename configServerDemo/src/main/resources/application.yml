server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NikkoBrand2001/configServerV2
          search-paths: dealerShip_configurations
          clone-on-start: true
          default-label: main

